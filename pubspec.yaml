name: ecopatrol_mobile
description: "A Flutter project with SQFlite for local storage"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '^3.9.2'
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  flutter_riverpod: ^2.5.0

  shared_preferences: ^2.3.2

  sqflite: ^2.3.0
  path: ^1.8.0
  path_provider: ^2.1.2

  image_picker: ^1.1.2
  geolocator: ^11.0.0
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true